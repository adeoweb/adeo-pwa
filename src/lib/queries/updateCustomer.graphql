#import "../fragments/customerDetails.graphql"

mutation updateCustomer($input: CustomerInput!) {
    updateCustomer(input: $input) {
        customer {
            ...CustomerDetails
        }
    }
}
