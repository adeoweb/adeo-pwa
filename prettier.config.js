const config = {
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'none',
    arrowParens: 'avoid',
    bracketSpacing: true,
    proseWrap: 'always',
    printWidth: 80
};

module.exports = config;

// TODO: Check import order settings. Check again!
